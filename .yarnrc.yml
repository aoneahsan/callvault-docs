# Docusaurus needs the classic node_modules linker (PnP breaks its plugin resolution).
nodeLinker: node-modules

# Owner directive: never quarantine just-published versions. Do NOT revert.
npmMinimalAgeGate: 0
