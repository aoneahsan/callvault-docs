# Dependencies
/node_modules

# Yarn (Berry) — keep releases/plugins, ignore cache + state
/.yarn/*
!/.yarn/patches
!/.yarn/plugins
!/.yarn/releases
!/.yarn/versions
.pnp.*
yarn-error.log

# Docusaurus build output + cache (all recoverable)
/build
/.docusaurus
/.cache-loader

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local
npm-debug.log*
yarn-debug.log*

# Owner standing ignores
*.ignore.*
project-record-ignore/
